

namespace GameEnums {
    public enum MonsterState {
        Idle, Attack, Moving, Death
    }

    public enum Debuff {
        Slow,
    }

    enum NexusSkills {
        ProtectShield, SlowCircle
    }

    enum PlayerSkills {
        Switching, RainFire
    }
}